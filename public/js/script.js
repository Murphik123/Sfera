// ============================================================
// ПЕРЕВОДЫ (Безопасное объявление через глобальный объект window)
// ============================================================
window.translations = window.translations || {
    ru: {
        logo_subtitle: "Национальная Цифровая Платформа",
        logout: "🚪 Выйти",
        back: "← Назад",
        messenger_title: "Мессенджер",
        tab_chats: "Чаты",
        tab_news: "Новости",
        tab_assistant: "Помощник",
        search_placeholder: "Поиск...",
        loading: "Загрузка...",
        select_chat: "Выберите чат",
        select_chat_desc: "Выберите собеседника или начните новый чат.",
        type_message: "Введите сообщение...",
        send: "Отправить",
        online: "онлайн",
        offline: "офлайн",
        assistant_name: "Помощник Sfera AI",
        assistant_status: "онлайн",
        assistant_hello: "Привет! Я ИИ-помощник платформы Sfera. Чем могу помочь?",
        audio_call: "📞 Аудиозвонок",
        video_call: "📹 Видеозвонок",
        news_no_items: "Нет новостей",
        news_title_1: "Обновление платформы",
        news_summary_1: "Вышла новая версия платформы Sfera.",
        news_title_2: "TM Coin растёт",
        news_summary_2: "Курс TM Coin вырос на 10%.",
        news_title_3: "Новые банковские услуги",
        news_summary_3: "Интегрированы новые кредитные возможности.",
        news_title_4: "Видеозвонки в мессенджере",
        news_summary_4: "Теперь можно совершать видеозвонки.",
        delete_dialog: "Удалить диалог",
        confirm_delete: "Вы уверены?",
        dialog_deleted: "Диалог удалён.",
        no_messages: "Нет сообщений",
        typing: "печатает...",
        theme_toggle: "🌙",
        create_group: "➕ Группа",
        create_group_title: "Создать группу",
        group_name: "Название группы",
        group_members: "Участники",
        create: "Создать",
        cancel: "Отмена",
        search_chat: "🔍 Поиск в чате",
        export_chat: "📥 Экспорт чата",
        export_success: "История чата экспортирована!",
        no_export: "Нет сообщений для экспорта.",
        favorite_add: "Добавлено в избранное",
        favorite_remove: "Удалено из избранного"
    },
    tm: {
        logo_subtitle: "Milli Sanly Platforma",
        logout: "🚪 Çykmak",
        back: "← Yza",
        messenger_title: "Söhbet",
        tab_chats: "Çatlar",
        tab_news: "Habarlar",
        tab_assistant: "Kömekçi",
        search_placeholder: "Gözleg...",
        loading: "Ýüklenýär...",
        select_chat: "Söhbeti saýlaň",
        select_chat_desc: "Çatdaşlaryňyzdan birini saýlaň.",
        type_message: "Habaryňyzy ýazyň...",
        send: "Iber",
        online: "onlaýn",
        offline: "offlaýn",
        assistant_name: "Kömekçi Sfera AI",
        assistant_status: "onlaýn",
        assistant_hello: "Salam! Men Sfera kömekçisi.",
        audio_call: "📞 Ses jaňy",
        video_call: "📹 Wideo jaň",
        news_no_items: "Habarlar ýok",
        news_title_1: "Platforma täzelenmeleri",
        news_summary_1: "Täze wersiýa çykdy.",
        news_title_2: "TM Coin bahasy ýokarlanýar",
        news_summary_2: "10% ýokarlandy.",
        news_title_3: "Täze bank hyzmatlary",
        news_summary_3: "Täze kredit mümkinçilikleri.",
        news_title_4: "Messenger-da wideo jaň",
        news_summary_4: "Indi wideo jaň edip bilersiňiz.",
        delete_dialog: "Söhbeti poz",
        confirm_delete: "Pozmak isleýärsiňizmi?",
        dialog_deleted: "Söhbet pozuldy.",
        no_messages: "Hiç hili habar ýok",
        typing: "ýazýar...",
        theme_toggle: "🌙",
        create_group: "➕ Topar",
        create_group_title: "Täze topar döret",
        group_name: "Toparyň ady",
        group_members: "Agzalar",
        create: "Döret",
        cancel: "Ýatyr",
        search_chat: "🔍 Gözleg",
        export_chat: "📥 Eksport",
        export_success: "Eksport edildi!",
        no_export: "Eksport üçin habar ýok.",
        favorite_add: "Söýlilere goşuldy",
        favorite_remove: "Söýlilerden aýryldy"
    },
    en: {
        logo_subtitle: "National Digital Platform",
        logout: "🚪 Logout",
        back: "← Back",
        messenger_title: "Messenger",
        tab_chats: "Chats",
        tab_news: "News",
        tab_assistant: "Assistant",
        search_placeholder: "Search...",
        loading: "Loading...",
        select_chat: "Select a chat",
        select_chat_desc: "Select a contact or start a new chat.",
        type_message: "Type your message...",
        send: "Send",
        online: "online",
        offline: "offline",
        assistant_name: "Sfera AI Assistant",
        assistant_status: "online",
        assistant_hello: "Hi! I'm Sfera assistant. How can I help you?",
        audio_call: "📞 Audio call",
        video_call: "📹 Video call",
        news_no_items: "No news available",
        news_title_1: "Platform Update",
        news_summary_1: "New version of Sfera is now available.",
        news_title_2: "TM Coin Growth",
        news_summary_2: "TM Coin rate increased by 10%.",
        news_title_3: "New Banking Services",
        news_summary_3: "New loan management features integrated.",
        news_title_4: "Video Calls in Messenger",
        news_summary_4: "Video calls are now active.",
        delete_dialog: "Delete dialogue",
        confirm_delete: "Are you sure?",
        dialog_deleted: "Dialogue deleted.",
        no_messages: "No messages yet",
        typing: "typing...",
        theme_toggle: "🌙",
        create_group: "➕ Group",
        create_group_title: "Create Group",
        group_name: "Group Name",
        group_members: "Members",
        create: "Create",
        cancel: "Cancel",
        search_chat: "🔍 Search in chat",
        export_chat: "📥 Export chat",
        export_success: "Chat history exported!",
        no_export: "No messages to export.",
        favorite_add: "Added to favorites",
        favorite_remove: "Removed from favorites"
    }
};

// ============================================================
// ОСНОВНОЙ МОДУЛЬ МЕССЕНДЖЕРА И ЗВОНКОВ
// ============================================================
document.addEventListener("DOMContentLoaded", async () => {
    const API_BASE = "https://sfera-1.onrender.com";
    let token = localStorage.getItem("token");
    let currentUser = null;
    let socket = null;
    let currentLang = localStorage.getItem("sfera_lang") || "ru";
    let activeChatId = null;
    let activeTab = "chats";
    let usersList = [];
    let peerConnection = null;
    let localStream = null;

    // Сбор элементов DOM
    const container = document.getElementById("messengerContainer");
    const themeToggleBtn = document.getElementById("themeToggle");
    const langBtn = document.getElementById("langBtn");
    const dialogsList = document.getElementById("dialogsList");
    const newsFeed = document.getElementById("newsFeed");
    const chatMessages = document.getElementById("chatMessages");
    const messageInput = document.getElementById("messageInput");
    const sendBtn = document.getElementById("sendBtn");
    const searchInput = document.getElementById("searchInput");
    const emojiBtn = document.getElementById("emojiBtn");
    const emojiPicker = document.getElementById("emojiPicker");
    const emojiGrid = document.getElementById("emojiGrid");
    const fileBtn = document.getElementById("fileBtn");
    const fileInput = document.getElementById("fileInput");
    const createGroupBtn = document.getElementById("createGroupBtn");
    const groupModal = document.getElementById("groupModal");
    const cancelGroupBtn = document.getElementById("cancelGroupBtn");
    const groupForm = document.getElementById("groupForm");
    const groupMembersSelect = document.getElementById("groupMembers");
    const backBtn = document.getElementById("backBtn");
    const sidebar = document.getElementById("sidebar");
    const exportChatBtn = document.getElementById("exportChatBtn");
    const audioCallBtn = document.getElementById("audioCallBtn");
    const videoCallBtn = document.getElementById("videoCallBtn");

    const emojis = ["😊", "😂", "👍", "🔥", "🎉", "❤️", "🙌", "😎", "🚀", "💡", "🤝", "📦", "💬", "✨"];

    // 1. Проверка авторизации и профиля
    async function initAuth() {
        if (!token) {
            window.location.href = "login.html";
            return;
        }
        try {
            const res = await fetch(`${API_BASE}/api/users/profile`, {
                headers: { "Authorization": `Bearer ${token}` }
            });
            if (!res.ok) throw new Error("Невалидный токен");
            const data = await res.json();
            currentUser = data.user || data;
            initSocketConnection();
        } catch (err) {
            console.error("Ошибка проверки сессии:", err);
            localStorage.removeItem("token");
            window.location.href = "login.html";
        }
    }

    // 2. Инициализация WebSocket
    function initSocketConnection() {
        if (typeof io === "undefined") return;
        socket = io(API_BASE);

        socket.on("connect", () => {
            socket.emit("register_user", currentUser._id || currentUser.id);
        });

        socket.on("receive_message", (msg) => {
            if (activeChatId && (String(msg.senderId) === String(activeChatId))) {
                appendMessageUI({ sender: "received", text: msg.text, file: msg.file, time: msg.time });
            }
            fetchDialogsAndUsers();
        });

        socket.on("user_status_change", ({ userId, online }) => {
            const user = usersList.find(u => String(u.id) === String(userId));
            if (user) user.online = online;
            renderDialogs(searchInput ? searchInput.value : "");
        });

        // WebRTC Сигналинг звонков
        socket.on("incoming_call", async (data) => {
            const accept = confirm(`Входящий ${data.isVideo ? 'видео' : 'аудио'}звонок от пользователя. Принять?`);
            if (accept) {
                activeChatId = data.from;
                await startCall(data.isVideo, true, data.signal);
            } else {
                socket.emit("reject_call", { to: data.from });
            }
        });

        socket.on("call_accepted", async (signal) => {
            if (peerConnection) {
                await peerConnection.setRemoteDescription(new RTCSessionDescription(signal));
            }
        });

        socket.on("call_rejected", () => {
            alert("Вызов отклонен");
            endCall();
        });

        socket.on("call_ended", () => {
            alert("Звонок завершен");
            endCall();
        });
    }

    // 3. Загрузка реальных диалогов и всех пользователей
    async function fetchDialogsAndUsers() {
        try {
            const res = await fetch(`${API_BASE}/api/chat/dialogs`, {
                headers: { "Authorization": `Bearer ${token}` }
            });
            if (res.ok) {
                const data = await res.json();
                usersList = data.users || data.dialogs || [];
                renderDialogs();
            }
        } catch (e) {
            console.warn("Бэкенд недоступен, фолбэк на локальное состояние", e);
        }
    }

    // 4. Инициализация переводов
    function updateTranslations() {
        const langData = window.translations[currentLang] || window.translations.ru;
        document.querySelectorAll("[data-i18n]").forEach(el => {
            const key = el.getAttribute("data-i18n");
            if (langData[key]) el.textContent = langData[key];
        });
        document.querySelectorAll("[data-i18n-placeholder]").forEach(el => {
            const key = el.getAttribute("data-i18n-placeholder");
            if (langData[key]) el.placeholder = langData[key];
        });
        document.querySelectorAll("[data-i18n-title]").forEach(el => {
            const key = el.getAttribute("data-i18n-title");
            if (langData[key]) el.title = langData[key];
        });
        if (langBtn) langBtn.textContent = currentLang.toUpperCase();
    }

    if (langBtn) {
        langBtn.addEventListener("click", () => {
            currentLang = currentLang === "ru" ? "tm" : currentLang === "tm" ? "en" : "ru";
            localStorage.setItem("sfera_lang", currentLang);
            updateTranslations();
            renderDialogs();
        });
    }

    // 5. Переключение темы
    if (themeToggleBtn && container) {
        themeToggleBtn.addEventListener("click", () => {
            container.classList.toggle("light-theme");
            themeToggleBtn.textContent = container.classList.contains("light-theme") ? "☀️" : "🌙";
        });
    }

    // 6. Выпадающий список вкладок
    const tabsDropdown = document.getElementById("tabsDropdown");
    const dropbtn = document.getElementById("dropbtn");
    const dropdownContent = document.getElementById("dropdownContent");
    const currentTabLabel = document.getElementById("currentTabLabel");

    if (dropbtn && tabsDropdown) {
        dropbtn.addEventListener("click", (e) => {
            e.stopPropagation();
            tabsDropdown.classList.toggle("show");
        });
        document.addEventListener("click", () => tabsDropdown.classList.remove("show"));
    }

    if (dropdownContent) {
        dropdownContent.querySelectorAll("button").forEach(btn => {
            btn.addEventListener("click", (e) => {
                dropdownContent.querySelectorAll("button").forEach(b => b.classList.remove("active"));
                btn.classList.add("active");
                activeTab = btn.getAttribute("data-tab");
                if (currentTabLabel) currentTabLabel.textContent = btn.textContent;
                if (tabsDropdown) tabsDropdown.classList.remove("show");

                if (activeTab === "chats") {
                    if (dialogsList) dialogsList.style.display = "block";
                    if (newsFeed) newsFeed.style.display = "none";
                    renderDialogs();
                } else if (activeTab === "news") {
                    if (dialogsList) dialogsList.style.display = "none";
                    if (newsFeed) newsFeed.style.display = "block";
                    renderNews();
                } else if (activeTab === "assistant") {
                    openAssistantChat();
                }
            });
        });
    }

    // 7. Отрисовка списка диалогов
    function renderDialogs(filter = "") {
        if (!dialogsList) return;
        dialogsList.innerHTML = "";
        const langData = window.translations[currentLang] || window.translations.ru;
        const filtered = usersList.filter(d => d.name.toLowerCase().includes(filter.toLowerCase()));
        
        if (filtered.length === 0) {
            dialogsList.innerHTML = `<div class="empty-state"><p>${langData.no_messages}</p></div>`;
            return;
        }

        filtered.forEach(dialog => {
            const item = document.createElement("div");
            item.className = `dialog-item ${activeChatId === dialog.id ? 'active' : ''}`;
            item.innerHTML = `
                <div class="dialog-avatar ${dialog.isGroup ? 'group' : ''}">
                    ${dialog.isGroup ? '👥' : dialog.name.charAt(0)}
                    ${dialog.online && !dialog.isGroup ? '<div class="online-dot"></div>' : ''}
                </div>
                <div class="dialog-info">
                    <div class="name">${dialog.name} ${dialog.role === 'admin' ? '🛡️' : ''}</div>
                    <div class="last-msg">${dialog.lastMsg || ''}</div>
                </div>
                ${dialog.unread > 0 ? `<div class="unread-badge">${dialog.unread}</div>` : ''}
            `;
            item.addEventListener("click", () => openChat(dialog));
            dialogsList.appendChild(item);
        });
    }

    // 8. Открытие диалога
    async function openChat(dialog) {
        activeChatId = dialog.id;
        dialog.unread = 0;
        renderDialogs(searchInput ? searchInput.value : "");

        const langData = window.translations[currentLang] || window.translations.ru;
        const nameEl = document.getElementById("chatName");
        const statusEl = document.getElementById("chatStatus");
        const avatarEl = document.getElementById("chatAvatar");

        if (nameEl) nameEl.textContent = dialog.name;
        if (statusEl) statusEl.textContent = dialog.online ? langData.online : langData.offline;
        if (avatarEl) avatarEl.textContent = dialog.isGroup ? '👥' : dialog.name.charAt(0);

        if (chatMessages) {
            chatMessages.innerHTML = "";
            try {
                const res = await fetch(`${API_BASE}/api/chat/${dialog.id}`, {
                    headers: { "Authorization": `Bearer ${token}` }
                });
                if (res.ok) {
                    const messages = await res.json();
                    messages.forEach(msg => appendMessageUI(msg));
                }
            } catch (err) {
                console.warn("Локальная загрузка истории");
                (dialog.messages || []).forEach(msg => appendMessageUI(msg));
            }
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        if (window.innerWidth <= 768 && sidebar) {
            sidebar.style.display = "none";
        }
    }

    function appendMessageUI(msg) {
        if (!chatMessages) return;
        const msgDiv = document.createElement("div");
        msgDiv.className = `message ${msg.sender}`;
        
        let html = '';
        if (msg.username) html += `<div class="msg-username">${msg.username}</div>`;
        html += `<div>${msg.text}</div>`;
        
        if (msg.file) {
            html += `<div class="file-attachment">📎 <a href="${msg.file.url}" download="${msg.file.name}">${msg.file.name}</a></div>`;
        }
        
        html += `<div class="msg-time">${msg.time}</div>`;
        msgDiv.innerHTML = html;
        chatMessages.appendChild(msgDiv);
        chatMessages.scrollTop = chatMessages.scrollHeight;
    }

    if (backBtn && sidebar) {
        backBtn.addEventListener("click", () => {
            sidebar.style.display = "flex";
        });
    }

    // 9. Отправка сообщений
    async function sendMessage() {
        if (!messageInput) return;
        const text = messageInput.value.trim();
        if (!text && (!fileInput || !fileInput.files.length)) return;

        const now = new Date();
        const timeStr = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
        
        const newMsg = {
            sender: "sent",
            text: text,
            time: timeStr
        };

        if (fileInput && fileInput.files.length > 0) {
            const file = fileInput.files[0];
            newMsg.file = { name: file.name, url: URL.createObjectURL(file) };
            fileInput.value = "";
        }

        appendMessageUI(newMsg);
        messageInput.value = "";

        // Отправка через Socket.io и REST API
        if (activeChatId && socket) {
            socket.emit("send_message", {
                recipientId: activeChatId,
                text: newMsg.text,
                file: newMsg.file,
                time: timeStr
            });

            try {
                await fetch(`${API_BASE}/api/chat/send`, {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json",
                        "Authorization": `Bearer ${token}`
                    },
                    body: JSON.stringify({ recipientId: activeChatId, text: newMsg.text })
                });
            } catch (e) {
                console.warn("Ошибка сохранения сообщения на бэкенде");
            }
        } else if (activeTab === "assistant") {
            const langData = window.translations[currentLang] || window.translations.ru;
            setTimeout(() => {
                appendMessageUI({
                    sender: "received",
                    text: langData.assistant_hello,
                    time: timeStr
                });
            }, 800);
        }
    }

    if (sendBtn) sendBtn.addEventListener("click", sendMessage);
    if (messageInput) {
        messageInput.addEventListener("keypress", (e) => {
            if (e.key === "Enter") sendMessage();
        });
    }

    // 10. WebRTC Звонки (Аудио и Видео)
    async function startCall(isVideo = false, isIncoming = false, incomingSignal = null) {
        if (!activeChatId) return alert("Выберите собеседника для звонка");
        
        const configuration = { iceServers: [{ urls: "stun:stun.l.google.com:19302" }] };
        peerConnection = new RTCPeerConnection(configuration);

        try {
            localStream = await navigator.mediaDevices.getUserMedia({ audio: true, video: isVideo });
            localStream.getTracks().forEach(track => peerConnection.addTrack(track, localStream));
        } catch (e) {
            console.warn("Медиаустройства недоступны или запрос отклонен");
        }

        peerConnection.onicecandidate = (event) => {
            if (event.candidate && socket) {
                // Передача ICE кандидатов
            }
        };

        if (isIncoming && incomingSignal) {
            await peerConnection.setRemoteDescription(new RTCSessionDescription(incomingSignal));
            const answer = await peerConnection.createAnswer();
            await peerConnection.setLocalDescription(answer);
            socket.emit("answer_call", { to: activeChatId, signal: answer });
        } else {
            const offer = await peerConnection.createOffer();
            await peerConnection.setLocalDescription(offer);
            socket.emit("call_user", { userToCall: activeChatId, signalData: offer, isVideo });
        }
    }

    function endCall() {
        if (localStream) {
            localStream.getTracks().forEach(track => track.stop());
        }
        if (peerConnection) {
            peerConnection.close();
            peerConnection = null;
        }
        if (socket && activeChatId) {
            socket.emit("end_call", { to: activeChatId });
        }
    }

    if (audioCallBtn) audioCallBtn.addEventListener("click", () => startCall(false));
    if (videoCallBtn) videoCallBtn.addEventListener("click", () => startCall(true));

    // 11. Эмодзи и файлы
    if (emojiGrid && emojiPicker && messageInput) {
        emojis.forEach(emo => {
            const btn = document.createElement("button");
            btn.textContent = emo;
            btn.addEventListener("click", () => {
                messageInput.value += emo;
                emojiPicker.classList.remove("active");
            });
            emojiGrid.appendChild(btn);
        });
    }

    if (emojiBtn && emojiPicker) {
        emojiBtn.addEventListener("click", (e) => {
            e.stopPropagation();
            emojiPicker.classList.toggle("active");
        });
        document.addEventListener("click", () => emojiPicker.classList.remove("active"));
    }

    if (fileBtn && fileInput) {
        fileBtn.addEventListener("click", () => fileInput.click());
    }

    // 12. Поиск по чатам
    if (searchInput) {
        searchInput.addEventListener("input", (e) => {
            if (activeTab === "chats") renderDialogs(e.target.value);
        });
    }

    // 13. Создание группы
    if (createGroupBtn && groupMembersSelect && groupModal) {
        createGroupBtn.addEventListener("click", () => {
            groupMembersSelect.innerHTML = "";
            usersList.filter(d => !d.isGroup).forEach(user => {
                const opt = document.createElement("option");
                opt.value = user.id;
                opt.textContent = user.name;
                groupMembersSelect.appendChild(opt);
            });
            groupModal.classList.add("active");
        });
    }

    if (cancelGroupBtn && groupModal) {
        cancelGroupBtn.addEventListener("click", () => groupModal.classList.remove("active"));
    }

    if (groupForm) {
        groupForm.addEventListener("submit", (e) => {
            e.preventDefault();
            const groupNameEl = document.getElementById("groupName");
            const name = groupNameEl ? groupNameEl.value.trim() : "";
            if (!name) return;

            const newGroup = {
                id: Date.now(),
                name: name,
                isGroup: true,
                online: true,
                unread: 0,
                messages: []
            };

            usersList.unshift(newGroup);
            if (groupModal) groupModal.classList.remove("active");
            groupForm.reset();
            renderDialogs();
            openChat(newGroup);
        });
    }

    // 14. Новости
    function renderNews() {
        if (!newsFeed) return;
        const langData = window.translations[currentLang] || window.translations.ru;
        newsFeed.innerHTML = `
            <div class="news-item">
                <div class="title">${langData.news_title_1}</div>
                <div class="summary">${langData.news_summary_1}</div>
                <div class="meta"><span>05.08.2026</span><span>SFERA Admin</span></div>
            </div>
            <div class="news-item">
                <div class="title">${langData.news_title_2}</div>
                <div class="summary">${langData.news_summary_2}</div>
                <div class="meta"><span>04.08.2026</span><span>Finance</span></div>
            </div>
        `;
    }

    // 15. Помощник Sfera AI
    function openAssistantChat() {
        if (dialogsList) dialogsList.style.display = "none";
        if (newsFeed) newsFeed.style.display = "none";
        activeChatId = "assistant";
        
        const langData = window.translations[currentLang] || window.translations.ru;
        const nameEl = document.getElementById("chatName");
        const statusEl = document.getElementById("chatStatus");
        const avatarEl = document.getElementById("chatAvatar");

        if (nameEl) nameEl.textContent = langData.assistant_name;
        if (statusEl) statusEl.textContent = langData.assistant_status;
        if (avatarEl) avatarEl.textContent = "🤖";

        if (chatMessages) {
            chatMessages.innerHTML = "";
            appendMessageUI({
                sender: "received",
                text: langData.assistant_hello,
                time: "00:00"
            });
        }
    }

    // 16. Экспорт чата
    if (exportChatBtn) {
        exportChatBtn.addEventListener("click", () => {
            const dialog = usersList.find(d => d.id === activeChatId);
            if (!dialog || !dialog.messages || !dialog.messages.length) return;

            const dataStr = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(dialog.messages, null, 2));
            const downloadAnchor = document.createElement("a");
            downloadAnchor.setAttribute("href", dataStr);
            downloadAnchor.setAttribute("download", `chat_${dialog.name.replace(/\s+/g, '_')}.json`);
            document.body.appendChild(downloadAnchor);
            downloadAnchor.click();
            downloadAnchor.remove();
        });
    }

    // Запуск приложения
    updateTranslations();
    await initAuth();
    await fetchDialogsAndUsers();
});
